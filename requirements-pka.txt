# Optional stack for Tools → pKa Predictor (pkasolver + PyTorch Geometric).
#
# Use ONE Python environment (your main .venv or system Python). Do not create separate
# .venvs/pka or admet-ai environments.
#
# Install (recommended):
#   Windows:  scripts\install_pytorch_pka.ps1
#   macOS/Linux:  bash scripts/install_pytorch_pka.sh
#
# Manual order (same as the scripts):
#   pip install -r requirements-pytorch-cpu.txt
#   pip install torch-scatter torch-sparse -f https://data.pyg.org/whl/torch-2.5.1+cpu.html
#   pip install -r requirements-pka.txt

pkasolver @ git+https://github.com/mayrf/pkasolver.git
torch-geometric==2.0.1
svgutils
