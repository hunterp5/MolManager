# Optional stack for Tools → Predict Permeability (GNN-MTL / Chemprop).
#
# Windows: default ``pip install torch`` often fails to load c10.dll (WinError 1114),
# especially from background threads. Install the official **CPU** build first (same as
# requirements-pka.txt), then:
#
#   pip uninstall torch torchvision torchaudio -y
#   pip install torch==2.5.1 torchvision==0.20.1 --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements-permeability.txt
#
# Model weights (not in git):
#
#   python scripts/bootstrap_gnn_mtl_model.py

# Pin torch+torchvision together (mismatched versions break Lightning/chemprop import).
# Use numpy<2 with rdkit-pypi (molmanager core already pins this).
numpy>=1.24,<2
chemprop>=2.1.0,<2.3
lightning>=2.0,<3
torch==2.5.1
torchvision==0.20.1
