# Optional stack for Tools → Predict Permeability (GNN-MTL / Chemprop).
#
# Requires the same CPU PyTorch 2.5.1 stack as pKa (install first):
#   scripts\install_pytorch_pka.ps1   (Windows)
#   bash scripts/install_pytorch_pka.sh
#
# Model weights (not in git):
#   python scripts/bootstrap_gnn_mtl_model.py

numpy>=1.24,<2
chemprop>=2.1.0,<2.3
lightning>=2.0,<3
