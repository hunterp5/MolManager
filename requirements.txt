# MolManager — complete Python dependency list.
# From the repo root with your virtual environment active:
#   pip install -U pip
#   pip install -r requirements.txt
#   pip install -e .
#
# Includes the desktop app, pKa predictor (PyTorch + pkasolver), permeability (Chemprop),
# docking helpers (Meeko), and development tools (pytest).

--extra-index-url https://download.pytorch.org/whl/cpu
--find-links https://data.pyg.org/whl/torch-2.5.1+cpu.html

# --- Desktop app and chemistry core ---
PyQt5>=5.15
# Embedded 3D structure viewer (View in 3D); without it, the app opens the browser instead.
PyQtWebEngine>=5.15
# Keep Plotly in the v5 line for broader Qt WebEngine compatibility.
plotly>=5.0,<6
# RDKit wheels (rdkit-pypi) are built against NumPy 1.x; NumPy 2.x causes _ARRAY_API / import failures.
numpy>=1.24,<2
pandas>=2.0
scipy>=1.10
sqlalchemy>=2.0
pubchempy>=1.0.4
chembl_webresource_client>=0.10.9
rdkit-pypi>=2022.9.5
scikit-learn>=1.3
umap-learn>=0.5.5

# --- PyTorch (CPU 2.5.1; pKa and permeability tools) ---
torch==2.5.1
torchvision==0.20.1
torch-scatter
torch-sparse

# --- pKa predictor (Tools → pKa Predictor) ---
pkasolver @ git+https://github.com/mayrf/pkasolver.git
torch-geometric==2.0.1
svgutils

# --- Permeability prediction (Tools → Predict Permeability / GNN-MTL) ---
chemprop>=2.1.0,<2.3
lightning>=2.0,<3

# --- Docking (Tools → Docking → Generate .pdbqt) ---
meeko>=0.5.0

# --- Development / CI ---
pytest>=7.4,<9
