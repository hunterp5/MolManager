# MolManager — desktop core (no PyTorch / Chemprop / docking ML stack).
# Prefer this for day-to-day table work and CI smoke installs:
#   pip install -U pip
#   pip install -r requirements-core.txt
#   pip install -e ".[dev]"
#
# For pKa, permeability, and docking Python helpers, use requirements.txt instead
# (or pip install -e ".[pka,permeability,docking]" after installing a matching torch).

# --- Desktop app and chemistry core ---
PyQt5>=5.15
# Embedded 3D structure viewer (View in 3D); without it, the app opens the browser instead.
PyQtWebEngine>=5.15
# Keep Plotly in the v5 line for broader Qt WebEngine compatibility.
plotly>=5.0,<6
# RDKit wheels are built against NumPy 1.x; NumPy 2.x causes _ARRAY_API / import failures.
numpy>=1.24,<2
pandas>=2.0
scipy>=1.10
sqlalchemy>=2.0
pubchempy>=1.0.4
chembl_webresource_client>=0.10.9
rdkit>=2022.9.5
scikit-learn>=1.3
umap-learn>=0.5.5
