"""
Non-Qt domain helpers (session I/O, file contracts).

Reserved for logic that should not live in ``ui/`` or ``workers/``. Most behavior
still lives next to its UI; add modules here when a boundary is worth enforcing.
"""
