# Byte-compiled / optimized
__pycache__/
*.py[cod]
*$py.class
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
.venv/
.venvs/
venv/
ENV/
env/

# Pytest / coverage / type check / Ruff
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.mypy_cache/
.ruff_cache/

# IDE (machine-local; keep .cursor/rules if you version team rules)
.idea/

# Cursor (machine-local; keep project rules in version control)
.cursor/*
!.cursor/rules/
!.cursor/rules/**

# Optional CLI binaries placed under resources/bin (keep README + .gitkeep)
molmanager/resources/bin/**/*
!molmanager/resources/bin/**/
!molmanager/resources/bin/**/.gitkeep

# GNN-MTL weights (download via scripts/bootstrap_gnn_mtl_model.py)
molmanager/resources/models/**/*.pt
!molmanager/resources/models/**/
!molmanager/resources/models/**/README.md
!molmanager/resources/models/**/.gitkeep

# Local large or throwaway inputs at repo root (remove lines if you want these tracked)
fda_approved_full.SDF
export_test.sdf
BindingDB_*.sdf
BindingDB_*.SDF

# Local training logs / scratch outputs
lightning_logs/

# Local scratch exports and sessions
test_export.sdf
test_session.cms
testcsv.csv

# Local scratch docking / PDB(QT) artifacts
/out
/samples/

# Project planning notes / changelog drafts (keep out of distributed repo)
ChemManager v* update.txt
BACKLOG.md

# Editor / OS junk
.~lock.*
*~
Thumbs.db
.DS_Store
